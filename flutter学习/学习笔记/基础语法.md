基本和java一致但是输出是用print并且不同数据类型之间并不能直接相加而是使用
void greetPerson(String name, int age) {
  // 方法 1：字符串插值 (最推荐)
  print("hello, $name age: $age");
  
  // 方法 2：转换类型
  // print("hello, " + name + "age: " + age.toString()); 
}
数据结构:
//data structures

/*

1:List:ordered collections of elements, can have duplicates.

2:set:underordered collections of uniqe elements

3:Map:stored as key-value pairs

*/

// List<int> numbers=[1,2,3];

// //numbers[0]->1

// List<String>names=["Mitch","sharon","Vince"];
// void printNumbers(){

//   for(int i=0;i<numbers.length;i++){

//     print(numbers[i]);

//   }

// }
// void printNames(){

//   for(int i=0;i<names.length;i++){

//     print(names[i]);

//   }

// }

Set<String> uniqueNames={"Mitch","Sharon","Vince"};

Map user={

  'name':"Mitch Koko",

  'age':27,

  'height':180,

};

//user['age'] -> Mitch Koko
